#include<stdio.h>

//Enum for Tokens
typedef enum tok{

}Token;

//Enum for NonTerminals
typedef enum nterm{

}NonTerminal;


//Enum for Terminals
typdef enum term{

}Terminal;


Token stringToToken(char* token);
char* tokenToString(Token t);
